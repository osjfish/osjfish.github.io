# 智能阅卷应用 · 状态与架构速览（压舱文档，供后续会话直接继承，不必再翻完整对话）

## 定位与交付
- 网页版答题卡系统，对标「答题卡助手 Pro v4.3.0」（Flutter+OpenCV），单文件 HTML + 本地 lib/。
- 线上：https://yuyewei.com/yuejuan.html（apps-server git 自动部署）；仓库：osjfish/osjfish.github.io。
- 约定（AGENTS.md）：每次改动 commit(中文)→push origin main(GitHub备份)→push server main(自动部署)。
- 数据：IndexedDB 'yuejuan_db'（classes/students/templates/exams/pages/crops/revs），无后端。

## 核心闭环
模板(设计/预设/PDF/二维码) → 班级学生(粘贴/Excel) → 考试(向导) → 扫描识别(上传→OMR→复核) → 按题阅卷(AI建议) → 成绩分析(统计/CSV/xlsx/JSON)。

## 关键设计（已实现，勿随意改语义）
- 二维码最小化：'DTZ1|2|模板ID|考号位数|页数|版式指纹'（42字符/29模块/1.17mm每格；34mm 右上角；仅首页 1 个；几何全部由批内模板提供；指纹=fnv1a(compactSliceFor level3 JSON)，打印后改模板→识别告警"版式指纹不匹配"）。解析兼容旧 v3(flag 3)/旧JSON(1/0)。
- 卡面：6锚点(4大+2小@标题行)；准考证号仅首页(可选每页 regEveryPage)：数字序号→"准考证号"→手写格→0-9涂号(列中心对齐)；无使用说明文字；页脚"第n页,共m页"；内容区底=h-36，续页起点38mm。
- 作文格 grid-cn：行间1.2mm缝隙，右侧缝隙内每满100字标注(跨页连续)；可选题目栏(titleLine，居中半长横线，续页不重复)；"每行格数 cols/总格数 count"可配置(count优先rows)；分页=逐页填满(不均衡)。
- 选择题：组{first,count,choices,cols,label,choicesQ(题级覆盖,如15:5)}；渲染/识别共用 groupQChoices/qBubbleGeom(数字区11mm后均分)；每题分值 exam.perQScore；多选 exams.qMulti+答案数组，判分=集合比对(全对满分，漏/多=0)；verdict: OK/MULTI(choice='AC'集合)/BLANK/REVIEW。
- 识别管线：灰度→2%-98%分位拉伸→自适应二值化(k,win)→页面级Otsu阈值(收集allInks后回填判定)→QR读取(整页/cropViaH透视校正)→锚点：全局四边形匹配(globalSolids+pickQuad,容差0.65-1.55)优先→缺角pickTriSmart(小锚定向+仿射tplIdx)→窗口法兜底→单应/仿射→行级同步条→OMR→主观题透视切割(cropViaH)。
- 复核：定位失败(手动4角重定位)、考号异常(改判regOverride+pageId级联)、涂点待复核(A-F/空白/多涂→revs)；识别框选可视化(pageMarkedImage,绿/红橙/灰)。
- 阅卷台：随机顺序/键盘(数字Enter←→F/G/Del)/快分/缩略带/批改标注图/AI建议(callVision+严格扣分提示+dimScores求和覆盖)。
- 首页=6步流程墙；全局无emoji(文本化)；设置页：AI(deepseek/doubao/glm/custom)/OMR灵敏度/备份(完整含图base64还原、最小仅元数据)。
- 上传绑定 bindFileInput 先快照再清空(FileList活对象)；相机拍摄：扫描页「拍照识别」= getUserMedia 实时取景(逐帧锚点检测+4点自动拍) + 失败降级系统相机 capture 输入；Excel按列头映射(姓名/考号必填；≤200人)；导出CSV(BOM)/xlsx/JSON。

## 有意保留的边界
模糊→待复核不静默乱判；模板删除→各页友好提示；手机强反光/大角度→引导手动定位；三点定位为仿射近似(有告警)。

## 文件与调试
- 主文件 D:\App\Apps\yuejuan.html（~212KB，内联单一IIFE）。lib/：jspdf.umd.min.js、qrcode.js、jsQR.js、pdf.min.js+worker、xlsx.full.min.js。
- 测试脚手架 D:\App\_test（puppeteer-core+系统Edge；本地 python -m http.server 8901 目录 D:\App\Apps 起 sv；脚本 flow/multi/scene2/minqr/nosim/v5/v6/mp 等）。
- 调试钩子 window.__yjDbg（recognizeCanvas/layoutTemplate/groupQChoices/qBubbleGeom/cropViaH/layoutHash/qrPackBytes/…）。
- 近期提交：5474f80(多选+Otsu) → 21b987d(二维码减负) → c8d6596(布局+鲁棒性+题级分/选项) → 8db4d71(移除模拟扫描) → 1c65cc3(全局锚点) → 07b38ad(上传FileList) → c486c68(Pro实践+QR二进制) → 卷面调整系列(767b853/2331fb5/e714ea3/dc5c962/0d106c6) → fb44b4b(准考证号仅首页)。

## 改代码注意
- 内嵌HTML大量 '+' 拼接与转义引号；批处理替换易踩 '+" 开头导致的 JSX 误解析、forEach().join() 类事故(已修类似3处)。改完必 node --check 抽取主脚本 + 本地 E2E。
- 工具函数不可嵌套进 layoutTemplate(曾致 groupQChoices 局部化崩溃)。
- 编辑前先 read；替换用唯一上下文锚点。
