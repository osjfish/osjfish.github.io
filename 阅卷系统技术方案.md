# 网页版智能阅卷系统 · 完整技术方案

> 对标应用：答题卡助手 Pro v4.3.0（Android）。目标：用纯前端（HTML+JS，无后端）复刻其核心闭环——**生成答题卡 → 打印 → 上传扫描件/照片 → 客观题自动识别 → 主观题切割 → 按题人工阅卷 + AI 辅助 → 成绩合成与分析导出**。
> 本文档面向实施者，含全部技术细节、数据结构、算法伪代码、提示词与验收标准。
> 工作区：`D:\App\Apps`，交付物：单文件应用 `yuejuan.html`（仓库现有风格：单文件 HTML + `lib/` 本地库，`build.sh` 自动收进导航页）。

---

## 0. 参照应用逆向分析结论

对两个 APK 做了静态拆解（unzip + 资源/Dart 符号分析，业务代码经百度加固不可读，但模块结构与资源完整可见）：

### 0.1 技术栈（Pro 与 Lite 基本一致）

| 层 | 实现 | 对本方案的启示 |
|---|---|---|
| UI 框架 | Flutter（Dart AOT 编译进 `libapp.so`），TDesign 组件 | 无关，网页版自有 UI |
| 识别核心 | **OpenCV 4**（22MB 原生库）+ **ncnn**（腾讯端侧 NN 推理，5MB） | 经典图像处理为主、本地小模型为辅。网页版用 JS canvas 复刻经典部分即可 |
| 防护 | 百度加固（classes.dex 为壳） | 无法（也无需）读其算法细节 |
| 数据 | 本地 SQLite(sqflite) + 云端 API | 见 0.3 |
| 登录 | 阿里一键登录 SDK | 网页版 MVP 不做账号 |

### 0.2 Pro 版完整模块图谱（由 Dart 包名还原，即功能清单）

- **答题卡**：`module_answer_card` —— 创建/列表/预览/**可视化编辑器**（编辑条含：选择题组 `choice_group`、填空题组 `fillblanks_group`、解答/主观题组 `writtenresponse_group`、**中文作文格 `composition_ch`**、**英文作文格 `composition_en`**、页管理、标题编辑框）；`sheet_print` 打印；`sheet_drawing` 工具层（`sheet_image_drawing` 图片绘制 / `sheet_pdf_drawing` PDF 绘制）。
- **班级学生**：`module_class` —— 班级列表、学生列表、Excel 导入（`read_excel`，内置《考生信息表.xlsx》模板）、手动添加。
- **考试**：`module_exam` —— 创建考试、**文件夹+面包屑导航**、**答案录入页**（选择题 key 录入）、班组选择、Pro/Lite 类型切换。
- **扫描阅卷**：`module_sheet_reader` —— 扫描主页面（相机取景实时检测答题卡 `sheet_detector_view`）、识别中对话框、识别确认页、识别设置、**批改标注图查看器**（`score_marked_image_viewer`，阅卷后在卷面图上落分）。
- **阅卷**：`module_score/answer_card_mark` —— 按题阅卷工作台（**横屏/竖屏两套布局**）、阅卷进度、阅卷历史。
- **成绩**：成绩列表/明细（按题型分组视图）/**成绩分析**（客观题分析、主观题分析、筛选器、饼图组件）/分析导出/学生个体统计。
- **模板分享**：`module_share` —— 答题卡模板分享/接收/保存（走云端 `/api/sheet`）。
- **云 API 全套**（Pro 用 `/api`，Lite 用 `/lite/api`）：user / my_class / student_manage / exam / answer_card / exam_answer_card / score / score_analysis / sheet_share / version / feedback。模型层有 `geometry.dart`（几何：点/矩形/变换）、`examinee_model`、`score_mark_model` 等。

### 0.3 Pro 与 Lite 差异

| 项 | Lite v3.5.5 | Pro v4.3.0 |
|---|---|---|
| 云端 | `/lite/api`，部分本地 | `/api` 全套云同步（班级/考试/成绩/模板分享） |
| 答题卡编辑 | 固定模板 | 可视化拖拽编辑器 + 中/英文作文格 |
| 阅卷 | 基础 | 进度/历史/横竖屏/批改标注图 |
| 变现 | 百度广告渠道 | 付费升级（`upgrade_dialog`） |
| native 差异 | — | 仅多 `libdartjni.so` |

### 0.4 样例答题卡实测要点（抽取内置 `sample.jpg` 与 `anchor0-3.png`）

1. **6 个定位点**：四角大实心黑方块（约 6×6mm，带白边框圈）+ 标题行两侧两个小方块，抗单角遮挡。
2. **同步条（timing mark）**：考号每位数字列右侧、每个选择题块左缘，各有一列与行对齐的小黑条（约 1×2mm）——纸张拉伸时按行校正的关键。
3. **考号区**：手写数字格 + 0-9 涂号行（样卡 5 位）。
4. **客观题**：方框涂点（内印 A/B/C/D），10 行一组 × 4 组 = 40 题，组间留白。
5. **主观题**：矩形作答框 + 横线（填空题区 30 分）。
6. 页脚「第1页,共2页」；注意事项明确"定位点周围禁止涂写"。
7. **卡上没有二维码**：模板在 App 内选择后扫描。我们的网页版改为**二维码自描述**（见 §4.6），省掉选模板步骤，同时保留"批内手选模板"为降级路径。

---

## 1. 目标与范围

### 1.1 MVP 功能清单（对应 Pro 模块）

| Pro 功能 | 网页版实现 | 期 |
|---|---|---|
| 答题卡生成/编辑/打印 | 模板参数化生成（编辑器先做"表单式"而非拖拽式）+ PDF 下载 | 一期 |
| 班级/学生/Excel 导入 | IndexedDB 存储 + xlsx/粘贴导入 | 一期 |
| 考试管理 + 答案录入 | 考试列表（平铺，文件夹后置）+ 客观题答案录入 | 一期 |
| 扫描识别（客观题+考号） | 上传 PDF/图片 → OMR 自动判分 | 二期 |
| 主观题切割 | 按区域坐标裁剪每生每题 | 三期 |
| 按题阅卷工作台 | 网页版阅卷台（响应式，对应横竖屏） | 三期 |
| 成绩列表/分析/导出 | 成绩册 + 每题得分率/分布 + CSV | 三/四期 |
| AI 辅助 | 单题裁剪图 → 建议分+理由（复用 pigai.html 的视觉链路） | 四期 |
| 批改标注图 | 裁剪图角落落分合成"已批图" | 四期 |
| 模板分享 | 导出/导入模板 JSON 文件（不走云） | 后置 |
| 云账号/多端同步/协作阅卷 | **明确不做**（需后端），数据本地 | — |

### 1.2 设计原则

1. **宁可待复核，不静默乱判**——一切识别不确定项进人工复核队列。
2. **零回归**：新应用独立文件，不影响 pigai.html 等现有应用。
3. **本地优先**：全部数据 IndexedDB，提供导出/导入备份；断网可用（AI 功能除外）。
4. 所有识别阈值可在设置页调整，默认值见 §5。

---

## 2. 总体架构

### 2.1 形态与选型

- 交付：`D:\App\Apps\yuejuan.html` 单文件（预计 3500-5000 行），静态资源放 `lib/`。无需构建步骤，与仓库现有 30+ 个单文件应用一致；`build.sh` 只扫 `*.html` 的 `<title>` 自动进导航页，title 定为 `智能阅卷`。
- 依赖库（全部本地化进 `lib/`，禁止 CDN——现有应用均为本地库模式）：

| 库 | 用途 | 备注 |
|---|---|---|
| `pdf.min.js` + worker | PDF→页面图像 | **已有**，pigai.html 同款用法 |
| `jspdf.umd.min.js` | 答题卡导出 PDF | 新增，MIT，~350KB |
| `qrcode-generator` | 生成版式二维码 | 新增，MIT，单文件 ~50KB（只生成不解析） |
| `jsQR` | 扫描页上解析二维码 | 新增，MIT，单文件 ~40KB |
| `xlsx.full.min.js` | 名单 xlsx 导入 | 可选（一期可用 CSV/粘贴兜底），MIT ~800KB，若嫌大可只做 CSV |

- 不用框架、不用打包器；代码内按模块分段注释组织（同 pigai.html 风格）。

### 2.2 数据流总览

```
【生成侧】模板参数 → 版式JSON → canvas渲染(mm→px) → 二维码(版式JSON) → PDF下载
【识别侧】上传PDF/图片 → pdf.js渲染 → jsQR读版式(或批内选模板) → 锚点检测
        → 单应校正 → 同步条行校验 → OMR判涂(考号+客观题) → 待复核项
【阅卷侧】主观题区域逆变换裁剪 → IndexedDB → 按题阅卷工作台(人工/AI建议) → 判分
【合成侧】客观自动分+主观阅卷分 → 成绩册 → 统计分析 → CSV/JSON导出
```

### 2.3 页面结构（单文件内 tab 切换）

`首页(考试列表) / ①新建考试(班级+答案+选模板) / ②模板设计器 / ③扫描识别 / ④阅卷工作台 / ⑤成绩 / 设置(AI、阈值、数据备份)`

---

## 3. 数据模型与存储

### 3.1 IndexedDB（库名 `yuejuan_db`，版本 1）

| objectStore | keyPath | 索引 | 内容 |
|---|---|---|---|
| `classes` | id | — | `{id,name,createdAt}` |
| `students` | id | byClass | `{id,classId,no,name,note}` |
| `templates` | id | — | 版式 JSON（§4.5 完整 schema） |
| `exams` | id | — | `{id,name,classId,templateId,answers:{题号:选项},subjective:{题号:满分/评分要点/参考答案},perScore,status,createdAt}` |
| `pages` | id | byExam | `{id,examId,studentNo,pageNo,blob(JPEG原页),meta{定位状态,二维码,告警}}` |
| `crops` | `[examId,studentNo,qno]` | byExamQno | `{examId,studentNo,qno,blob,thumb,score,aiScore,aiReason,marked(已批图blob),markedBy,flag}` |
| `scores` | examId | — | `{examId,byStudent:{考号:{objective:{题号:verdict},subjective:{题号:分},total}}}` |

- localStorage 只放：AI 设置（provider/key/model，格式照抄 pigai.html 的 `pigai_ai`）、OMR 阈值、UI 偏好。
- 图片一律存 Blob（JPEG q0.85），`pages` 每页 2000px 长边（约 300-600KB），`crops` 每张 600-800px（约 50-120KB）。一场 50 人×2 页考试 ≈ 100 张页图 + 200 张裁剪 ≈ 60-90MB，IndexedDB 配额充足（Chrome 默认允许到磁盘的 60%）。
- 备份：设置页提供「导出全部数据（JSON+图片 base64 打包为 .json）/导入」。提示用户：页图可由原始扫描件重建，最小备份只需元数据+成绩。

### 3.2 关键约定

- **学生主键 = 考号**（卡上涂写），学号为展示字段。考号位数随模板（默认 5，可配 4-9）。
- 判分 verdict 枚举：`对/错/空白/多涂/待复核/已改判`。改判记录 `markedBy:'manual'`。

---

## 4. 答题卡模板与生成器（一期）

### 4.1 坐标系统

- **设计坐标一律用毫米（mm）**，原点=页面左上角。渲染换算：`px = mm × dpi / 25.4`。屏幕预览 96dpi，PDF 内嵌 300dpi 位图或矢量。
- 纸型参数表：

| 纸型 | mm (宽×高) | 300dpi 像素 |
|---|---|---|
| A4 竖 | 210×297 | 2480×3508 |
| A4 横 | 297×210 | — |
| A3 | 297×420 | 3508×4961 |
| B4 | 257×364 | 3035×4299 |
| 8开(近似) | 260×368 | — |

- 生成器 MVP：A4 竖版单页/双页；其余纸型只是参数，代码天然支持。

### 4.2 版面元素规格（全部有默认值，可调）

**锚点（定位标记）×6**：四角 4 个大锚 + 标题行两侧 2 个小锚。
- 大锚：外白框 9×9mm（线宽 0.4mm），内实心黑方块 6×6mm；中心距纸角 12mm。
- 小锚：外框 5×5mm，内实心 3×3mm。
- 识别容差设计：锚点检测窗口=预期位置 ±3% 页宽；面积过滤区间按 300dpi 换算后 ±50%；方形度（w/h）0.7~1.4。

**同步条（timing marks）**：
- 每个选择题组左缘外侧 3mm 处，一列 1×2mm 实心黑条，条中心与该组每行涂点行中心对齐（行距见下）。
- 考号每位数字列右缘 3mm 处同样一列（与 0-9 十行对齐）。
- 作用：纸张受潮/打印拉伸时，用相邻两条实测位置对行内涂点做**逐行线性校正**（§5.7）。

**考号区**：位数可配（默认 5，范围 4-9）。每位一列：顶部手写格 8×8mm，下方 0-9 十行涂点（方框 3×3mm，内印数字 7pt，行距 4mm）。

**选择题组**：参数 = {起始题号, 题数, 选项数(4/5), 列数, 每列行数(默认10)}。
- 涂点：4.2×2.8mm 方框，内印选项字母 8pt；同行题间距（横向相邻题组起点）默认 22mm（含 4 选项+间隙）；行距 5mm；组间距 8mm；组左侧放同步条列。
- 版式自动排版：给定纸型与各区高度，从上到下依次排：标题区(18mm) → 考号区(~55mm) → 客观题区 → 主观题区 → 页脚(10mm)。空间不足自动分页（页脚输出"第n页,共m页"）。

**主观题区**：参数 = {题号, 满分, 高度mm, 行数, 类型}。
- `lines`（横线填空/解答）：矩形框 0.3mm 线，内部横线间距 8mm，浅灰(#999)。
- `grid-cn`（中文作文格）：8×8mm 方格，每行 20 格，格间 0.2mm 线，首行标"100字"类计数提示（仿 Pro `composition_ch`）。
- `grid-en`（英文四线格）：行高 5mm 四线，仿 Pro `composition_en`。
- 每个主观题区左上角印 `题号+（满分N分）`。

**页眉/页脚**：标题（居中，可编辑文字）；页脚中央"第{n}页,共{m}页"；左下放**版式二维码**（§4.6）；页脚四角与大锚之间留 ≥8mm 空白。

### 4.3 模板设计器 UI（表单式，非拖拽——拖拽编辑器后置）

左侧表单、右侧 1:1 A4 预览（canvas，96dpi）：
1. 基本信息：模板名、纸型、页数、标题文字。
2. 考生信息：考号位数、是否含班级涂号列（可选项，MVP 默认无）。
3. 选择题：行式编辑器（起始题号/题数/选项数/每列行数），可加多组。
4. 主观题：行式编辑器（题号/满分/类型 lines|grid-cn|grid-en/行数或高度）。
5. 「重新排版」按钮 → 自动布局算法重排 → 预览刷新；所有元素允许后续手动微调上下间距（mm 输入）。
6. 校验规则：题号连续、总高度不超页面、主观题区最小高度 ≥30mm。

### 4.4 绘制与导出

- 核心函数 `renderTemplate(tpl, dpi) → canvas`：按 §4.2 逐元素绘制；同一函数用于（a）预览（96dpi）（b）PDF 内嵌位图（300dpi）（c）生成用于标定的基准图。
- PDF 导出：jsPDF，页尺寸按纸型 mm，`addImage(canvas.toDataURL('image/jpeg',0.92))` 整页位图（300dpi A4 单页 JPEG ≈ 1-2MB，可接受；后续可改矢量绘制减小体积）。
- 打印提示（弹窗文案）：「请选择"实际大小/100%"打印，取消"适应页面"」。锚点+单应可吸收均匀缩放，但"适应页面"的非等比缩放超出校正范围时会被检出并标"版式异常"。

### 4.5 版式 JSON Schema（模板存储 + 二维码载荷，同一结构）

```js
{
  v: 1,                       // schema 版本
  id: 'tpl_xxxx', name: '英语答题卡A4',
  paper: 'A4', orient: 'portrait', w: 210, h: 297,
  regDigits: 5,
  pages: [{
    anchors: [ {x,y,s}, ×6 ],            // 实心方块左上角+边长(mm)，顺序：左上/右上/右下/左下/小左/小右
    qr: {x,y,size} | null,               // 本页二维码位置
    title: '英语答题卡',
    reg: {x, y, cols: 5, digitTopY: 0, rowsY0: 0, dy: 4, size: 3},  // 考号涂区：列x、首行y、行距
    regTiming: {dx: 3, size: [1,2]},     // 考号同步条相对列偏移
    objective: [ {
      first: 1, count: 10, choices: 4, rows: 10,
      x0: 20, y0: 90,       // 首列首行涂点左上角(mm)
      dxq: 22, dy: 5, dxc: 6.5, size: [4.2, 2.8],
      timing: {dx: -3, w: 1, h: 2}
    }, ... ],
    subjective: [ {qno: '二', score: 30, type: 'lines', x, y, w, h, lineGap: 8}, ... ],
    footer: '第{n}页,共{m}页'
  }],
  perScore: 2               // 客观题每题分值（可被 exam 覆盖）
}
```

### 4.6 二维码自描述（本方案相对 Pro 的改进点）

- 内容：`DTZ1|` + base64( deflate-raw( JSON.stringify(版式中与本页相关部分) ) )。压缩用浏览器原生 `CompressionStream('deflate-raw')`（Chrome/Edge/Safari 16.4+ 均支持），不支持则不压缩直接 base64。
- 载荷大小评估：A4 单页典型版式 JSON ≈ 1.2KB → deflate 后 ≈ 500B → 二维码 Version 10-12（57×57 模块），打印 15×15mm、纠错等级 M，扫描/打印存活率高。若超出则去掉主观题坐标细节（保留锚点+客观题+考号），主观题区域改由「批内模板匹配」补全。
- 载荷**不含答案**（保密与防作弊），答案只存在本地 exams 表。
- 降级路径：二维码读不出 → 使用该批次考试在应用内绑定的模板（等价于 Pro 的做法），并在页面上提示「第 n 页未读到码，已按模板 X 解析」。

### 4.7 内置模板预设

- 小学客观题卡：40 题×4 选 + 3 道解答框 + 考号 5 位（≈ Pro sample.jpg 版式）。
- 语文作文卡：考号 + 2 题基础题 + 800 字作文格。
- 英语卡：60 题×4 选 + 作文四线格。
- 空白自定义。模板可导出/导入 JSON 文件（对应 Pro 模板分享的离线版）。

---

## 5. 识别管线（二期，核心）

### 5.1 输入与预处理

- 支持多选文件（PDF/图片混选）。PDF 每页经 pdf.js 渲染，长边统一 **2200px**（A4 ≈ 187dpi，300dpi 原扫可降采样，涂点直径仍 >20px）；图片直接解码并同规则缩放。
- 全部转灰度 Float32Array 处理，canvas 仅用于最终展示/裁剪。

### 5.2 处理流程（每页 12 步）

```
1 缩放至长边2200px → 2 灰度化 → 3 自适应二值化(§5.3)
→ 4 整页 jsQR 找版式二维码(§4.6)；找到→载入版式；否则用批内模板并记录告警
→ 5 估计初始缩放比 s = 页宽mm→px；推算6锚点预期位置
→ 6 逐锚点窗口搜索(±3%页宽)：连通域取最大实心黑块，校验方形度/面积 → 质心
   任一大锚失败→用小锚顶替重试；大锚<4个→整页标记「定位失败」进复核，终止本页
→ 7 4大锚 → 单应矩阵H(§5.5)；2小锚做残差校验(重投影误差>1.5mm→告警)
→ 8 同步条行校正(§5.7)：每列同步条实测行位置 → 行级偏移表
→ 9 考号区OMR：逐位判涂(§5.6) → 考号；未涂全/两位同涂→「考号异常」
→ 10 客观题OMR：逐题判涂 → verdict
→ 11 输出页结果{pageId, studentNo, perQ[], flags[]}，页图按JPEG存 IndexedDB
→ 12 批次组装：按考号归组页；缺页/重号/无考号 → 复核清单
```

### 5.3 自适应二值化（积分图法）

```
窗口半径 r=15px；对每像素：mean = 积分图矩形均值；
binary = gray < mean * 0.75 ? 1 : 0
```
- 积分图一次 O(n) 构建，整体 O(n)；2200px 页 ≈ 480 万像素，JS 单线程约 200-400ms。
- 系数 0.75 与窗口 15 进设置页（「识别灵敏度」预设三档：宽松0.8/标准0.75/严格0.7）。

### 5.4 锚点检测

- 在预期位置的方形窗口（边长=锚边长×4）内做 BFS 连通域（二值图已就绪），取面积最大块；
- 校验：`0.7 ≤ w/h ≤ 1.4`，面积 ∈ [0.5, 2]×理论值，实心率（块面积/外接矩形面积）≥ 0.8（实心方块≈1，可排除"回"字边框）；
- 质心=(Σx/n, Σy/n)。失败处理见流程第 6 步。

### 5.5 单应变换（4 点 → 4 点）

用 DLT 直接解 8 参数（齐次归一后 h33=1）。对 4 对点 `(x_i,y_i)→(u_i,v_i)` 建立线性方程组 `A·h = 0`（8×8），高斯消元求解。参考实现（~35 行 vanilla JS）：

```js
function solveHomo(src, dst){ // src/dst: [{x,y}×4]
  const A=[], b=[];
  for(let i=0;i<4;i++){
    const {x,y}=src[i], {x:u,y:v}=dst[i];
    A.push([x,y,1,0,0,0,-u*x,-u*y]); b.push(u);
    A.push([0,0,0,x,y,1,-v*x,-v*y]); b.push(v);
  }
  // 8x8 高斯消元求 h，返回 (x,y)->(u,v) 的映射闭包
  const h = gaussSolve8(A,b);
  return (x,y)=>({
    u:(h[0]*x+h[1]*y+h[2])/(h[6]*x+h[7]*y+1),
    v:(h[3]*x+h[4]*y+h[5])/(h[6]*x+h[7]*y+1)
  });
}
```

- 使用方向：**版式坐标(mm) → 页面像素坐标**，即把 dst 作为版式点（乘 dpi 因子无所谓，单应天然吸收）。识别测量时不需要真正旋转图像，全部用逆映射按坐标采样；仅主观题裁剪时对该矩形区域做逐像素逆映射重采样（区域小，开销可忽略）。
- 手机拍照的透视由单应吸收；扫描件只会出现仿射级形变，精度冗余足够。

### 5.6 涂点判读（OMR）

- 对每个涂点：逆映射得到中心像素 (cx,cy)，采样半径 `r = 0.8 × 涂点半宽像素`；
- `ink = (binary==1 像素数) / ROI 总数`（直接在二值图上数）；
- 判定（阈值进设置页，默认）：

| ink | 判定 |
|---|---|
| ≥ 0.45 (`high`) | 已涂 |
| ≤ 0.25 (`low`) | 空白 |
| (0.25, 0.45) | **模糊 → 该题进待复核**（不猜） |

- 整题合成：恰好 1 个"已涂" → 该选项；0 个已涂且无模糊 → 空白（若该题在答案中本来可选填，则按错/空白计）；≥2 个已涂 → 多涂；含模糊 → 待复核（附该题涂点 ink 值列表）。
- 考号位判定同上，但规则更严：模糊一律「考号异常」进复核。

### 5.7 同步条行校正（抄自样卡设计，抗纸张拉伸）

- 对每个同步条预期位置：在 x±2mm 窗口内沿 y 方向找二值图中连续黑段质心 → 实测行 y；
- 相邻两条之间的涂点行 y 值 = 线性插值；顶部/底部之外按边缘条外推；
- 若某列同步条缺失 ≥2 条 → 放弃该列校正（退回纯单应映射）并记告警。
- 该步把"整体单应"升级为"单应+行级微校"，是打印伸缩下保持高识别率的关键。

### 5.8 待复核体系（汇总）

| 告警 | 来源 | 复核 UI 动作 |
|---|---|---|
| 定位失败 | 锚点<4 | 展示整页缩略图，手动点 4 角重定位（复用标定点击逻辑） |
| 未读二维码 | jsQR 失败 | 提示按批内模板解析（可忽略） |
| 考号异常/重号 | OMR | 放大考号区图 + 数字键盘改判 |
| 涂点模糊/多涂 | OMR | 展示该题涂点放大图 + ink 值条，点选判 |
| 版式异常 | 重投影误差/缩放比异常 | 建议重扫该页 |
| 缺页/无考号页 | 批次组装 | 指派考号或忽略 |

### 5.9 性能预算

单页（2200px）：二值化 0.3s + 锚点 0.05s + OMR（~300 涂点）0.05s + jsQR 0.2s ≈ **0.6-1s/页**。批量采用逐页 `await` + 进度条（同步阻塞主线程可接受；>200 页再考虑 Worker，MVP 不做）。

---

## 6. 主观题切割（三期上半）

- 对每页每个主观题区域：用单应逆映射把矩形 4 角映射到像素坐标，逐像素双线性重采样裁剪（区域典型 400×300mm → 输出长边 800px，JPEG q0.85）。
- 存 `crops` 表，key `[examId,studentNo,qno]`；同时生成 160px 缩略图供列表。
- 作文格区域输出长边 1200px（格子密度大）。
- 切割在识别流程第 11 步后同页完成；重扫某页时按 pageId 级联删除重建。

---

## 7. 阅卷工作台（三期下半，对应 Pro answer_card_mark）

### 7.1 交互

- 入口：考试 → 「开始阅卷」→ 题目选择页（每主观题显示：满分、已阅/总数、平均分、进度环）。
- 阅卷页（单栏响应式，Pro 的横竖屏两套布局在这里合并为一个自适应布局）：
  - 主区：当前学生该题裁剪图（可缩放）；
  - 评分条：0..满分 快捷按钮（步长可配 0.5/1），支持键盘 `0-9` 输入、`Enter` 下一份、`←/→` 前后翻份、`F` 标记疑难、`G` 标记优秀；
  - 顶部进度条 + 剩余数；底部缩略图胶带（前后 5 份）；
  - 「随机打乱」开关（默认开，降低宽严漂移——阅卷顺序与考号无关）；
  - 每次打分即时写 IndexedDB（无"保存"按钮）。
- MVP 单评；双评+仲裁（分差>阈值进仲裁队列）数据模型已预留（crops 表 `markedBy`/`flag`），后置。

### 7.2 批改标注图（对应 Pro score_marked_image_viewer）

- 打分后在裁剪图右下角合成半透明色块 + 分数（canvas 叠加，`marked` 字段），成绩明细页可查看"已批图"，导出报告可附。

---

## 8. AI 辅助阅卷（四期）

### 8.1 调用设计

- 复用 pigai.html 的 `callVision`（OpenAI 兼容 chat/completions + image_url base64，DeepSeek/豆包/GLM/自定义四服务商），**复制代码进本应用**（保持单文件自包含），设置页同款（provider/key/model/图片清晰度）。
- 触发：阅卷页每题一个「AI 建议」按钮 + 「批量 AI 预阅」按钮（对未阅卷的该题全部裁剪图顺序跑，2 路并发，可停止）。
- 请求：system + user(文本) + 1 张裁剪图。图片用 800px 档（省流 600/标准 800/高清 1100 三档）。

### 8.2 提示词（严格扣分式，全文照用）

system：
```
你是一位经验丰富、认真负责的阅卷老师。评分时，你是执行主流阅卷标准的严格阅卷人：每一分都必须有卷面依据，发现错误必须扣分，不得因"整体印象尚可"而免扣具体问题。所有输出使用简体中文。
```

user：
```
【题目】{题号，题面/作文题}
【满分】{score} 分
【评分要点】{exam.subjective[qno].要点，教师录入}
【参考答案】{可选}
【评分规则】
1. 先通读作答，把所有问题逐条列全（错别字、病句、计算错误、步骤缺失、漏答未答、未按要求等），不许遗漏、不许美化；
2. 评分采用"从满分往下扣"：每处问题按评分要点扣除对应分数，注明扣分原因；
3. 得分可以有小数，不得为凑整抬高分数；每一处扣分都要能在卷面上指出来；
4. 若要点中各维度标有分值，必须按维度给分（dimScores），总分为各维度之和。
【输出要求】只输出 JSON：{"problems":"问题清单","dimScores":{"维度":分}|null,"score":数字,"reason":"一句话总评"}，不要其他文字。
```

- 解析容错沿用 pigai.html 的 `parseResult`（剥 ```json 围栏、修尾逗号）。
- `dimScores` 存在时**总分以维度求和覆盖**（防虚高）。
- 返回后 UI 显示：建议分按钮（点击=采纳写入 score）+ 展开理由 + problems。教师可改分，`aiScore/aiReason` 永久留存对比（分析页可统计"AI 与人工分差"）。
- 严格度三档（宽松/适中/严格）作为规则 2 的前缀注入，默认适中；考试级「评分锚点」字段（教师贴往届样本与分数）追加在 user 末尾。可选「复核遍」：对 AI 初分追加一次复核调用（同 pigai 方案 §1.8）。

### 8.3 成本估算（标准档 800px，JPEG≈100KB ≈ 900 图片 token）

| 规模 | 调用数 | 总 token(约) | DeepSeek 视觉档费用(约) |
|---|---|---|---|
| 50 生×4 主观题 | 200 | ~40 万 | 几元人民币 |
| 50 生×作文整页(1200px) | 50 | ~25 万 | 几元人民币 |

- 全部可关（纯人工阅卷零成本）；「批量 AI 预阅」前弹确认显示预估调用量。

---

## 9. 成绩合成、分析与导出（三/四期）

- **总分 = 客观题(自动) + 主观题(人工/AI 采纳)**；每题可配分值（客观默认 `perScore`，主观录入时必填满分）。
- 成绩册：行=学生（按考号匹配 students，未匹配=「未识别」单独一组），列=各题得分/总分/班级排名。
- 分析页（对应 Pro score_analysis）：
  - 客观题：每题得分率条形图 + 错题排行（谁错选了什么，选项分布）；
  - 主观题：分数段直方图 + 平均分；AI/人工分差统计（四期）；
  - 班级整体：总分分布、优良合格率（阈值可配）。
  - 图表用纯 canvas 画（不引图表库，柱状/条形足够）。
- 导出：成绩册 CSV（BOM 头，Excel 直开，格式照抄 pigai.html `exportCSV`）、考试备份 JSON（元数据+成绩）、单人报告（可选，含已批图）。

---

## 10. 班级与考试管理

- 班级：列表/新增/删除；学生：CRUD + 两种导入——粘贴（每行`姓名 考号`，照抄 pigai.html `bulkAddStudents` 解析）与 xlsx（若引入 SheetJS；否则 CSV）。提供《考生信息表》示例下载（仿 Pro）。
- 考试：新建 = 选班级 + 选/建模板 + 录客观题答案（题号行内点选 A-D 快捷录入）+ 录主观题满分/要点 + 每题分值；列表平铺（文件夹/面包屑后置，对应 Pro exam_list breadcrumbs）；考试内状态机：`设置中 → 待扫描 → 待复核 n 项 → 阅卷中 → 已完成`。

---

## 11. 分期计划与验收标准

| 期 | 内容 | 工作量 | 验收标准 |
|---|---|---|---|
| 一 | 生成器：模板设计器 + renderTemplate + 二维码 + PDF 下载 + 班级/学生/考试管理 | 2 天 | 内置 3 预设可生成并正确打印；同模板两次生成像素一致；二维码 15mm 打印-扫描可读率 100% |
| 二 | 识别管线：上传→OMR→考号→复核面板 | 2 天 | 打印后扫描 20 份：定位成功 100%、涂卡判定与人工核对一致率 ≥98%、考号识别 ≥99%；模糊/多涂 100% 进复核而非误判 |
| 三 | 切割 + 阅卷工作台 + 成绩合成/CSV | 2 天 | 50 人×2 页流程走通；打分即存刷新不丢；CSV Excel 直开无乱码 |
| 四 | AI 辅助 + 标注图 + 分析页 | 1-2 天 | AI 建议与人工分差中位数 ≤ 满分 10%（严格档）；断网时除 AI 外全功能可用 |

- 手机拍照场景（非扫描）：验收单列——正上方拍照、四角锚点完整、无强反光条件下，同标准执行；不满足时进定位失败复核而非误判。

## 12. 风险与对策

1. **打印缩放**：提示 100% 打印；均匀缩放由单应吸收，非等比缩放被重投影校验检出 → 「版式异常」复核。
2. **拍照质量**：透视由单应吸收；强反光/大角度 → 定位失败复核。识别设置页给出拍照指引文案。
3. **存储**：图片只存 IndexedDB；清浏览器数据会丢 → 设置页醒目备份入口 + 每次考试完成弹一次备份提醒。
4. **涂卡质量**：阈值三档预设 + 复核兜底；卡面注意事项印上 Pro 同款提示语。
5. **二维码读不出**：降级批内模板，功能不受损。
6. **AI 费用失控**：默认关闭、批量前预估确认、三档清晰度。
7. **浏览器兼容**：目标 Chrome/Edge/Safari 近两年版本；`CompressionStream` 缺失时自动不压缩（二维码更大但可用）。

## 13. 附： pigai.html 经验直接沿用清单

- AI 设置弹窗/四服务商/测试连接：整段复制（`PROVIDERS`、`callVision`、`parseResult`、`withRetry`）。
- 学生粘贴导入、名单自动匹配（`bestStudent`）逻辑按考号改造复用。
- 评分偏宽修复结论已内化进 §8.2 提示词（去"鼓励为主"、扣分式、先列问题后给分、dimScores 求和覆盖、严格度、锚点、复核遍）——**勿在实施时改写措辞**。
- CSV 导出格式（BOM + 引号转义）照抄。

---

*文档版本 v1.0 · 2026-09-01 · 依据：答题卡助手 Lite v3.5.5 / Pro v4.3.0 静态逆向 + 样卡实测 + pigai.html 现有代码盘点*
